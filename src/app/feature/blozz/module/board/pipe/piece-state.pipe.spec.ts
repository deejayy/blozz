import { PieceStatePipe } from './piece-state.pipe';

describe('PieceStatePipe', () => {
  it('create an instance', () => {
    const pipe = new PieceStatePipe();
    expect(pipe).toBeTruthy();
  });
});
