import { MatrixWidthPipe } from './matrix-width.pipe';

describe('MatrixWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new MatrixWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
